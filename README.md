# ESP32-Rhythm-Game
This project is a real-time rhythm game built on the ESP32 system. Audio playback is synchronized with LED matrix animations, while user inputs are handled using GPIO interrupts for low-latency response. The system evaluates timing accuracy and generates scores based on player performance.
